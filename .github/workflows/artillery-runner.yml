name: "Artillery Load Test"

run-name: >
  Artillery Workflow: ${{ github.event.inputs.test_name }} ${{ github.event.inputs.run_name }}

# ============================================================
# PURPOSE:
# Automates load testing with Artillery for multiple applications.
# Features:
#   - Run parallel tests across multiple GitHub runners
#   - Aggregate results and generate dashboards
#   - Cleanup old reports/artifacts
# ============================================================

on:
  workflow_dispatch:
    inputs:
      test_type:
        description: "Choose test type: 'load' or 'cleanup'"
        required: true
        type: choice
        options: [load, cleanup]

      environment_name:
        description: "Select environment (test or prod)"
        required: true
        type: choice
        options: [test, prod]

      app_name:
        description: "Application to test (e.g., app1, app2)"
        required: true
        type: choice
        options: [app1, app2]

      runners_to_use:
        description: "Number of parallel runners to use (1–4)"
        required: true
        type: choice
        options: ['1','2','3','4']

      scenario_file:
        description: "YAML file for Artillery scenario"
        required: true
        default: "aws_signed_api.yml ,aws_api.yml"

      test_name:
        description: "Optional test name (blank = auto-generated)"
        required: false
        default: ""

      run_name:
        description: "Optional run name (blank = auto-generated)"
        required: false
        default: ""

      monitor_system:
        description: "Enable runner CPU/memory monitoring?"
        required: false
        type: choice
        options: ["true","false"]
        default: "false"

      cleanup_days:
        description: "Delete reports/artifacts older than X days (cleanup mode)"
        required: false
        default: "7"

      print_machine_info:
        description: "Print machine info before running?"
        required: false
        type: choice
        options: ["true","false"]
        default: "false"

# ============================================================
# Global environment variables used throughout all jobs
# ============================================================
env:
  ARTILLERY_VERSION: "2.0.21"
  UPLOAD_RESULTS: "true"
  SCRIPTS_DIR: "./applications/${{ github.event.inputs.app_name }}/scripts"
  DATA_DIR: "./applications/${{ github.event.inputs.app_name }}/data"

  # Python script environment variables
  PY_APP_NAME: ${{ github.event.inputs.app_name }}
  PY_TEST_NAME: ${{ github.event.inputs.test_name }}        
  PY_RUN_ID: ${{ github.event.inputs.run_name }}            
  PY_SLA: ""                                               
  PY_MODE: ""                                              
  PY_GRANULARITY: ""                                       
  PY_SHOW_OVERALL_METRICS: ""                              

# ============================================================
# JOB 1: Set test name dynamically
# ============================================================
jobs:
  set-test-name:
    runs-on: ubuntu-latest
    outputs:
      final_test_name: ${{ steps.determine.outputs.final_test_name }}
    steps:
      - id: determine
        run: |
          DATE_MMDDYYYY=$(date +%m%d%Y)
          DATE_YYYYMMDD=$(date +%Y%m%d)
          BASE_NAME="artillery"

          if [ -n "${{ github.event.inputs.test_name }}" ] && [ -n "${{ github.event.inputs.run_name }}" ]; then
            FINAL_NAME="${BASE_NAME}-${{ github.event.inputs.test_name }}-${{ github.event.inputs.run_name }}"
          elif [ -n "${{ github.event.inputs.test_name }}" ]; then
            FINAL_NAME="${BASE_NAME}-${{ github.event.inputs.test_name }}"
          elif [ -n "${{ github.event.inputs.run_name }}" ]; then
            FINAL_NAME="${BASE_NAME}-${{ github.event.inputs.run_name }}"
          else
            FINAL_NAME="${BASE_NAME}-test-${DATE_YYYYMMDD}-run_${DATE_MMDDYYYY}"
          fi

          echo "✅ Final test name: $FINAL_NAME"
          echo "final_test_name=$FINAL_NAME" >> $GITHUB_OUTPUT

# ============================================================
# JOB 2: Run Artillery tests in parallel
# ============================================================
  run-artillery:
    if: ${{ github.event.inputs.test_type == 'load' }}
    name: "Run Artillery Tests"
    runs-on: ubuntu-latest
    needs: set-test-name
    environment: ${{ github.event.inputs.environment_name }}
    strategy:
      fail-fast: false
      matrix:
        runner_index: [1, 2, 3, 4]
    steps:
      # -------------------------------
      # Set REPORT_DIR at top-level
      # -------------------------------
      - name: Set job environment variables
        run: |
          export REPORT_DIR="${{ github.workspace }}/_reports"
          mkdir -p "$REPORT_DIR"
          echo "REPORT_DIR=$REPORT_DIR" >> $GITHUB_ENV
          echo "REPORT_DIR location: $REPORT_DIR"

      # -------------------------------
      # Skip unused runners
      # -------------------------------
      - name: Skip Unused Runner
        if: ${{ matrix.runner_index > fromJSON(github.event.inputs.runners_to_use) }}
        run: |
          echo "Skipping runner ${{ matrix.runner_index }}"
          exit 0

      # -------------------------------
      # Checkout source code
      # -------------------------------
      - uses: actions/checkout@v4

      # -------------------------------
      # Optional: Print machine info
      # -------------------------------
      - name: Print Machine Info
        if: ${{ github.event.inputs.print_machine_info == 'true' }}
        run: |
          echo "===== Machine Info ====="
          nproc
          free -h
          uname -a
          echo "========================"

      # -------------------------------
      # Setup Python & dependencies
      # -------------------------------
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13.1"
      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas plotly numpy pyyaml

      # -------------------------------
      # Verify scenario YAML exists
      # -------------------------------
      - name: Check scenario YAML exists
        shell: bash
        run: |
          if [ ! -f "${{ env.SCRIPTS_DIR }}/${{ github.event.inputs.scenario_file }}" ]; then
            echo "Scenario file not found!"
            exit 1
          fi
          echo "Found scenario file: ${{ env.SCRIPTS_DIR }}/${{ github.event.inputs.scenario_file }}"

      # -------------------------------
      # Setup Node.js & Artillery
      # -------------------------------
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install Artillery
        run: |
          npm install -g artillery@${{ env.ARTILLERY_VERSION }}
          npm install aws4 @faker-js/faker csv-parse
          npm install --save-dev artillery-plugin-metrics-by-endpoint
          artillery --version

      # -------------------------------
      # Optional System Monitoring
      # -------------------------------
      - name: Start system monitoring (optional)
        if: ${{ github.event.inputs.monitor_system == 'true' }}
        shell: bash
        run: |
          LOG_FILE="${{ env.REPORT_DIR }}/runner-${{ matrix.runner_index }}-system.log"
          echo "timestamp,cpu_user,cpu_system,cpu_idle,mem_used,mem_free" > $LOG_FILE
          monitor() {
            while true; do
              ts=$(date +"%Y-%m-%d %H:%M:%S")
              cpu=$(mpstat 1 1 | awk '/Average/ {print $3","$5","$12}')
              mem=$(free -m | awk '/Mem:/ {print $3","$4}')
              echo "$ts,$cpu,$mem" >> $LOG_FILE
              sleep 180
            done
          }
          monitor & echo $! > /tmp/monitor_pid.txt
          echo "System monitoring started in background."

      # -------------------------------
      # Map environment variables dynamically
      # -------------------------------
      - name: Map Environment Variables Dynamically
        shell: bash
        run: |
          APP_UPPER=$(echo "${{ github.event.inputs.app_name }}" | tr '[:lower:]' '[:upper:]')
          BASE_URL="${!APP_UPPER_BASE_URL}"
          TARGET_HOST="${!APP_UPPER_TARGET_HOST}"
          AWS_REGION="${!APP_UPPER_REGION}"
          AWS_ACCESS_KEY_ID="${!APP_UPPER_ACCESS_KEY}"
          AWS_SECRET_ACCESS_KEY="${!APP_UPPER_SECRET_KEY}"
          echo "BASE_URL=$BASE_URL" >> $GITHUB_ENV
          echo "TARGET_HOST=$TARGET_HOST" >> $GITHUB_ENV
          echo "AWS_REGION=$AWS_REGION" >> $GITHUB_ENV
          echo "AWS_ACCESS_KEY_ID=$AWS_ACCESS_KEY_ID" >> $GITHUB_ENV
          echo "AWS_SECRET_ACCESS_KEY=$AWS_SECRET_ACCESS_KEY" >> $GITHUB_ENV
          echo "✅ Environment variables mapped for $APP_UPPER"

      # -------------------------------
      # Run Artillery Test
      # -------------------------------
      - name: Run Artillery Test
        id: run_test
        shell: bash
        run: |
          JSON_OUT="${{ env.REPORT_DIR }}/result-runner-${{ matrix.runner_index }}.json"
          HTML_OUT="${{ env.REPORT_DIR }}/report-runner-${{ matrix.runner_index }}.html"

          npx artillery run "applications/${{ github.event.inputs.app_name }}/scripts/${{ github.event.inputs.scenario_file }}" \
            --output "$JSON_OUT"
          npx artillery report "$JSON_OUT" --output "$HTML_OUT"
          echo "✅ Artillery reports generated at: ${{ env.REPORT_DIR }}"

      # -------------------------------
      # Stop system monitoring
      # -------------------------------
      - name: Stop system monitoring (optional)
        if: ${{ github.event.inputs.monitor_system == 'true' }}
        run: |
          if [ -f /tmp/monitor_pid.txt ]; then
            kill $(cat /tmp/monitor_pid.txt) || true
            rm /tmp/monitor_pid.txt
            echo "System monitoring stopped."
          fi

      # -------------------------------
      # Generate Python dashboard for runner
      # -------------------------------
      - name: Generate Python dashboard
        shell: bash
        run: |
          DASHBOARD_HTML="${{ env.REPORT_DIR }}/dashboard-runner-${{ matrix.runner_index }}.html"
          JSON_FILE="${{ env.REPORT_DIR }}/result-runner-${{ matrix.runner_index }}.json"
          python "${GITHUB_WORKSPACE}/utilities/generate_artillery_dashboard.py" \
            --json "$JSON_FILE" \
            --yaml "${{ env.SCRIPTS_DIR }}/${{ github.event.inputs.scenario_file }}" \
            --output "$DASHBOARD_HTML" || echo "⚠️ Dashboard generation failed"

      # -------------------------------
      # Package and upload runner results
      # -------------------------------
      - name: Package runner results
        shell: bash
        run: |
          ZIP_NAME="runner-${{ matrix.runner_index }}-results.zip"
          zip -r "$ZIP_NAME" "${{ env.REPORT_DIR }}" >/dev/null
          echo "✅ Packaged runner ZIP: $ZIP_NAME"

      - name: Upload runner artifact
        uses: actions/upload-artifact@v4
        with:
          name: artillery-results-runner-${{ matrix.runner_index }}
          path: runner-${{ matrix.runner_index }}-results.zip

# ============================================================
# JOB 3: Aggregate results after all runners finish
# ============================================================
  aggregate-reports:
    if: always()
    runs-on: ubuntu-latest
    needs: [set-test-name, run-artillery]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Collect runner artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./downloaded-artifacts

      - name: Merge all runner files into _reports
        shell: bash
        run: |
          REPORT_DIR="./_reports"
          mkdir -p "$REPORT_DIR"
          cp ./downloaded-artifacts/*/* "$REPORT_DIR"/ 2>/dev/null || true
          echo "✅ Merged all runner files into $REPORT_DIR"

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13.1"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas pyyaml plotly numpy

      - name: Generate consolidated dashboard
        shell: bash
        run: |
          JSON_FILES=$(find "$REPORT_DIR" -name '*.json' -type f | paste -sd "," -)
          FINAL_HTML="$REPORT_DIR/${{ needs.set-test-name.outputs.final_test_name }}-dashboard.html"
          if [ -n "$JSON_FILES" ]; then
            python "${GITHUB_WORKSPACE}/utilities/generate_artillery_dashboard.py" \
              --json "$JSON_FILES" \
              --yaml "${{ env.SCRIPTS_DIR }}/${{ github.event.inputs.scenario_file }}" \
              --output "$FINAL_HTML" || echo "⚠️ Consolidated dashboard failed"
            echo "✅ Consolidated dashboard: $FINAL_HTML"
          else
            echo "⚠️ No JSON files found, skipping consolidated dashboard"
          fi

      - name: Package consolidated results
        shell: bash
        run: |
          zip -r consolidated-results.zip _reports >/dev/null
          echo "✅ Packaged consolidated-results.zip"

      - name: Upload consolidated artifact
        uses: actions/upload-artifact@v4
        with:
          name: consolidated-artillery-results
          path: consolidated-results.zip

# ============================================================
# JOB 4: Cleanup old reports/artifacts
# ============================================================
  cleanup-artifacts:
    if: ${{ github.event.inputs.test_type == 'cleanup' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Delete old local reports
        run: |
          REPORT_DIR="./_reports"
          DAYS=${{ github.event.inputs.cleanup_days }}
          echo "🧹 Deleting reports older than $DAYS days from $REPORT_DIR"
          find "$REPORT_DIR" -type f -mtime +$DAYS -print -delete
          echo "✅ Local cleanup completed"

      - name: Delete old GitHub artifacts
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          DAYS=${{ github.event.inputs.cleanup_days }}
          ARTIFACTS=$(curl -s -H "Authorization: token $GITHUB_TOKEN" \
            "https://api.github.com/repos/${{ github.repository }}/actions/artifacts?per_page=100" \
            | jq -r '.artifacts[] | "\(.id) \(.created_at)"')
          NOW=$(date +%s)
          while read -r ID CREATED_AT; do
            ART_TS=$(date -d "$CREATED_AT" +%s)
            AGE=$(( (NOW - ART_TS) / 86400 ))
            if [ "$AGE" -gt "$DAYS" ]; then
              echo "Deleting artifact ID: $ID ($AGE days old)"
              curl -s -X DELETE -H "Authorization: token $GITHUB_TOKEN" \
                "https://api.github.com/repos/${{ github.repository }}/actions/artifacts/$ID"
            fi
          done <<< "$ARTIFACTS"
          echo "✅ GitHub artifact cleanup completed"
